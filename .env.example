# Required for signup — without this, signup is disabled
PC_INVITE_CODE=

# Required for visitor access — without this, visitor login is disabled
PC_VISITOR_PASSWORD=

# Required — secret key for signing session cookies
PC_SESSION_SECRET=
